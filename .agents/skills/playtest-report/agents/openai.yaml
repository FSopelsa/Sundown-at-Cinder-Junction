interface:
  display_name: "Playtest Report"
  short_description: "Generates a structured playtest report template or analyzes existing…"
  default_prompt: "Use $playtest-report to generates a structured playtest report template or analyzes existing playtest notes into a structured format. Use this to standardize playtest feedback collection and analysis."
policy:
  allow_implicit_invocation: true
