interface:
  display_name: "Sundown Project Status"
  short_description: "Assess what is playable and identify the next small milestone."
  default_prompt: "Use $project-stage-detect to assess Sundown's current playable state and recommend the next practical steps."
policy:
  allow_implicit_invocation: true
