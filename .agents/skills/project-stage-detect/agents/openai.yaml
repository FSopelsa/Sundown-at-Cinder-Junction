interface:
  display_name: "Project Stage Detect"
  short_description: "Automatically analyze project state, detect stage, identify gaps, and…"
  default_prompt: "Use $project-stage-detect to automatically analyze project state, detect stage, identify gaps, and recommend next steps based on existing artifacts. Use when user asks 'where are we in development', 'what stage are we in', 'full project audit'."
policy:
  allow_implicit_invocation: true
