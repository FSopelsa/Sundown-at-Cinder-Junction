{
  "name": "sundown-cinder-junction",
  "version": "0.1.0",
  "description": "A space-western elemental tower-defense prototype.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "vite": "8.0.16"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
